package cmd

import (
	"fmt"
	"github.com/spf13/cobra"
)

func init(){
    appendCommand(&cobra.Command{
        Use:    "{{.PackageName}}",
        Short:  "",
        Run:    run{{.StructName}},
        // Args:  cobra.ExactArgs(1), // 只允许且必须传 1 个参数
      })
}


func run{{.StructName}}(_ *cobra.Command, _ []string) {

	fmt.Println("success")
}
